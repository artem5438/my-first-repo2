# 🎬 Проект "НОРА" - Система управления кинотеатром

## Концепция и техническое описание

**Статус:** Концептуальная фаза

---

## 📋 Оглавление

1. [Общее описание проекта](#общее-описание-проекта)
2. [Функциональные требования](#функциональные-требования)
3. [Технические требования](#технические-требования)
4. [Архитектура системы](#архитектура-системы)
5. [Структура базы данных](#структура-базы-данных)
6. [Пользователи и роли](#пользователи-и-роли)
7. [Бизнес-логика](#бизнес-логика)
8. [Обязательные технологии](#обязательные-технологии)
9. [График разработки](#график-разработки)

---

## 🎯 Общее описание проекта

**"Нора"** - это веб-приложение для управления и продажи билетов в кинотеатре. Система позволяет пользователям регистрироваться, просматривать афишу, покупать билеты онлайн, отслеживать личный баланс баллов и управлять своими заказами.

### Основные характеристики:

- **Масштаб:** Небольшой кинотеатр (4-5 залов по 50 мест каждый)
- **Общая вместимость:** ~200-250 зрителей
- **Режим работы:** 2 сеанса в день на каждый фильм
- **Целевая аудитория:** Конечные пользователи (зрители) и администраторы

---

## 🎫 Функциональные требования

### 1. Регистрация и управление пользователями

#### Данные пользователя:

- Полное имя (ФИО)
- Email (уникальный)
- Номер телефона
- Дата рождения (для проверки возраста)
- Пароль (хеширование обязательно)
- Дата регистрации
- Дата последнего обновления профиля

#### Функциональность:

- Регистрация новых пользователей
- Автоматическое сохранение в БД
- Вход в систему (аутентификация)
- Просмотр и изменение личных данных
- **Без функций:** восстановление пароля, двухфакторная аутентификация (на будущее)

---

### 2. Система управления фильмами

#### Информация о фильме:

- Название
- Описание / сюжет
- Жанр (один или несколько)
- Режиссер
- Возрастное ограничение (0+, 6+, 12+, 16+, 18+)
- Продолжительность (в минутах)
- Постер / изображение обложки
- Дата начала проката
- Дата окончания проката

#### Источник данных:

- Загрузка из внешнего источника (например, TMDB API, IMDb или вручную через admin-панель)
- Возможность обновления информации

#### Функциональность:

- Просмотр афиши (список активных фильмов)
- Фильтрация по жанру, возрастному ограничению
- Поиск по названию
- Отображение расписания сеансов для каждого фильма

---

### 3. Система управления сеансами и местами

#### Сеансы:

- Каждый фильм показывается **2 раза в день** (примерно в 18:00 и 21:00)
- Сеанс привязан к конкретному залу
- Фиксированное время начала и окончания
- Возможность изменения расписания администратором

#### Залы:

- 4-5 залов в кинотеатре
- Каждый зал вмещает 50 мест
- Возможность разной конфигурации (опционально на будущее)

#### Места:

- Нумерация: ряд (A-E) + номер места (1-10)
- Состояние места:
  - Свободно
  - Зарезервировано
  - Продано
  - Зарезервировано (во время оформления заказа)

---

### 4. Система покупки билетов (онлайн)

#### Процесс покупки:

1. Пользователь выбирает фильм (в афише)
2. Выбирает дату и сеанс
3. Выбирает место(а) в зале
4. Переходит к оплате (имитация)
5. Система генерирует уникальный билет с QR-кодом

#### Имитация оплаты:

- Реальных денежных транзакций **нет**
- Система просто создает запись в БД о "совершенной покупке"
- Чек с QR-кодом отображается в личном кабинете
- Билет можно скачать/распечатать

#### Валидация:

- Проверка возраста пользователя перед покупкой (для рейтингов 16+, 18+)
- Проверка наличия свободных мест
- Проверка времени (нельзя купить билет на прошлый сеанс)

#### Цена билета:

- На данный момент **фиксированная** (различия в цене на будущее)
- Хранится в системе для расчета баллов

---

### 5. Система баллов и вознаграждений

#### Начисление баллов:

- **Правило:** 10 баллов за каждые 100 рублей потраченных денег
- Баллы начисляются **автоматически** после покупки билета
- Пример: билет за 200 рублей → +20 баллов

#### Срок действия баллов:

- Баллы полученные за одну покупку действуют **1 месяц**
- После истечения срока баллы сгорают
- Система должна отслеживать дату получения каждого балла

#### История транзакций:

- Запись всех операций начисления баллов
- Запись всех операций траты баллов
- Возможность просмотра истории в личном кабинете

#### Трата баллов:

- На момент разработки БД функционал **в стадии обсуждения**
- Предполагаемые варианты: скидка на билет, товары в кафе или сайте
- **В БД:** предусмотреть место для расширения функциональности

---

### 6. Система отмены билетов

#### Правила отмены:

- Билет можно отменить **не позже чем за 30 минут до начала сеанса**
- После этого времени отмена **невозможна**
- При отмене место автоматически становится свободным

#### Возврат баллов:

- При отмене билета баллы возвращаются на счет пользователя
- Логика учета (полный возврат, штраф или др.) — на обсуждение

#### Логирование:

- Запись информации об отмене (кто, когда, какой билет)
- Возможность просмотра истории отмен

---

### 7. Отчеты и аналитика

#### Статистика в БД:

- **Популярные фильмы** (по количеству проданных билетов, по рейтингу)
- **Загруженность залов** (процент заполнения по дням, по фильмам)
- **Финансовые показатели** (доход по дням, по фильмам)
- **Активность пользователей** (количество регистраций, покупок, отмен)

#### Представление отчетов:

- SQL-запросы (VIEW, SELECT с агрегацией)
- Возможность экспорта данных (опционально)
- Визуализация на уровне приложения (диаграммы, графики)

---

### 8. Проверка возраста

#### Механизм:

- При покупке билета на фильм с рейтингом 16+ или 18+ система проверяет дату рождения
- Если пользователю меньше требуемого возраста → покупка запрещена
- Система показывает сообщение об ошибке

#### Ограничения:

- Система основана на дате рождения (легко обмануть, но для курсача допустимо)
- Полная верификация (документы, биометрия) - не требуется

---

## 💻 Технические требования

### СУБД и инструменты:

- **СУБД:** PostgreSQL
- **Клиент:** DBeaver (для управления и тестирования БД)
- **Язык запросов:** SQL (DDL, DML, SELECT)

### Объем БД:

- **Минимум таблиц:** 10-15 (по требованию курсовой работы)
- **Ожидаемое количество:** 14-15 таблиц

### Нормализация:

- Третья нормальная форма (3NF) минимум
- Исключение избыточности и аномалий
- Использование внешних ключей для связей

### Целостность данных:

- Уникальные индексы (primary key, unique constraints)
- Проверки (check constraints)
- Ограничения на NULL-значения (not null)
- Внешние ключи (foreign key) с правилами каскадного удаления

---

## 🏗️ Архитектура системы

### Многоуровневая архитектура:

```
┌─────────────────────────────────────────┐
│     Веб-приложение (фронтенд)           │
│  (HTML, CSS, JavaScript / React / Vue)  │
└──────────────────┬──────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────┐
│         REST API (бэкенд)               │
│   (Node.js / Python / Java / Go и т.д.) │
│  • CRUD операции                        │
│  • Валидация данных                     │
│  • Бизнес-логика                        │
│  • Аутентификация / Авторизация         │
└──────────────────┬──────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────┐
│    PostgreSQL (база данных)             │
│  • 14-15 таблиц                         │
│  • Индексы и оптимизация                │
│  • Триггеры (опционально)               │
│  • Представления (VIEW) для отчетов     │
└─────────────────────────────────────────┘
```

### Компоненты:

1. **Фронтенд:** Интерфейс для пользователей (покупка билетов, личный кабинет)
2. **API:** REST endpoints для взаимодействия с БД
3. **Бизнес-логика:** Валидация, проверка возраста, начисление баллов
4. **БД:** PostgreSQL с полной структурой данных

---

## 📊 Структура базы данных

### Таблицы (ожидаемый список):

| № | Таблица          | Описание                                                                        |
| -- | ----------------------- | --------------------------------------------------------------------------------------- |
| 1  | `users`               | Зарегистрированные пользователи                           |
| 2  | `movies`              | Информация о фильмах                                                  |
| 3  | `genres`              | Жанры фильмов                                                               |
| 4  | `movie_genres`        | Связь фильм-жанр (M2M)                                                    |
| 5  | `halls`               | Залы кинотеатра                                                           |
| 6  | `seats`               | Места в залах                                                                |
| 7  | `sessions`            | Сеансы (фильм + зал + время)                                         |
| 8  | `session_seats`       | Состояние мест на конкретный сеанс                        |
| 9  | `orders`              | Заказы (покупка билетов)                                            |
| 10 | `tickets`             | Проданные билеты                                                         |
| 11 | `user_points_balance` | Текущий баланс баллов пользователя                       |
| 12 | `points_transactions` | История начисления/траты баллов                             |
| 13 | `cancellations`       | История отмен билетов                                                |
| 14 | `audit_log`           | Логирование критических действий (опционально) |
| 15 | `user_roles`          | Роли пользователей (user, admin) (опционально)              |

---

## 👥 Пользователи и роли

### Роли в системе:

#### 1. **Конечный пользователь (User)**

- Регистрация и авторизация
- Просмотр афиши
- Покупка билетов
- Просмотр личного кабинета
- Просмотр баланса баллов
- Отмена билетов
- История заказов и баллов

#### 2. **Администратор (Admin)**

- Всё, что может сделать User
- Добавление / удаление фильмов
- Управление сеансами и расписанием
- Управление залами и местами
- Просмотр отчетов и статистики
- Управление пользователями (на будущее)

### Аутентификация:

- Требуется email и пароль
- Пароль хешируется (bcrypt или подобное)
- Сессия или JWT-токен для поддержания состояния
- Проверка прав доступа на уровне API

---

## 🧮 Бизнес-логика

### 1. Процесс покупки билета:

```
Пользователь выбирает фильм
    ↓
Проверка возраста (если рейтинг 16+, 18+)
    ↓
Выбор сеанса и мест
    ↓
Проверка наличия свободных мест
    ↓
Проверка времени (не позже 30 минут до начала)
    ↓
Имитация оплаты (создание заказа)
    ↓
Создание билетов с уникальным ID / QR-кодом
    ↓
Начисление баллов (10 за 100 рублей)
    ↓
Обновление состояния мест (на "продано")
    ↓
Отправка подтверждения пользователю
```

### 2. Процесс отмены билета:

```
Пользователь запрашивает отмену
    ↓
Проверка времени (не позже 30 минут до начала)
    ↓
Если время прошло → ошибка "Отмену невозможно выполнить"
    ↓
Если время позволяет → создание записи об отмене
    ↓
Возврат баллов на счет пользователя
    ↓
Обновление состояния мест (на "свободно")
    ↓
Логирование операции
```

### 3. Управление баллами:

```
При покупке билета:
  Рассчитать баллы = (цена билета / 100) * 10
  ↓
  Создать запись в points_transactions (начисление)
  ↓
  Обновить user_points_balance (добавить баллы)
  ↓
  Установить дату истечения (текущая дата + 1 месяц)

При истечении срока действия:
  Найти баллы с истекшей датой
  ↓
  Создать запись в points_transactions (сгорание)
  ↓
  Обновить user_points_balance (вычесть баллы)
```

### 4. Проверка возраста:

```
При покупке билета на фильм с рейтингом 16+ или 18+:
  ↓
  Рассчитать возраст пользователя из дата_рождения
  ↓
  Если возраст < требуемый → отклонить покупку
  ↓
  Если возраст >= требуемый → разрешить покупку
```

---

## 🔧 Обязательные технологии

По требованию курсовой работы необходимо реализовать **3 технологии** из предложенного списка. Выбранные технологии:

### 1. ✅ REST API (обязательное)

#### Описание:

- Взаимодействие веб-приложения с PostgreSQL
- Endpoints для основных операций CRUD
- JSON для передачи данных
- Проверка прав доступа и валидация на уровне API

#### Ожидаемые endpoints:

**Аутентификация:**

- `POST /api/auth/register` - регистрация нового пользователя
- `POST /api/auth/login` - вход в систему
- `POST /api/auth/logout` - выход из системы

**Фильмы и сеансы:**

- `GET /api/movies` - список всех фильмов (с фильтрацией)
- `GET /api/movies/{id}` - информация о конкретном фильме
- `GET /api/sessions?movie_id=X` - сеансы для фильма
- `GET /api/sessions/{id}/seats` - состояние мест в сеансе

**Билеты и заказы:**

- `POST /api/orders` - создание новый заказ (покупка билета)
- `GET /api/orders/{id}` - детали заказа
- `GET /api/my-tickets` - мои билеты (для пользователя)
- `DELETE /api/tickets/{id}` - отмена билета

**Баллы:**

- `GET /api/points/balance` - текущий баланс баллов
- `GET /api/points/history` - история транзакций баллов

**Аналитика (для админа):**

- `GET /api/analytics/popular-movies` - популярные фильмы
- `GET /api/analytics/occupancy` - загруженность залов
- `GET /api/analytics/revenue` - доход по дням

#### Технологический стек (на выбор):

- **Node.js + Express** (JavaScript)
- **Python + Flask** или **FastAPI**
- **Java + Spring Boot**
- **Go + Gin**
- Или другой фреймворк на выбор

#### Документирование:

- Swagger / OpenAPI для описания API
- README с примерами запросов и ответов

---

### 2. ✅ Автоматическая репликация и бэкап

#### Описание:

- Настройка автоматического резервного копирования БД PostgreSQL
- Восстановление из бэкапа при необходимости
- Демонстрация процесса в документации

#### Реализация:

**Способ 1: pg_dump (встроенный инструмент PostgreSQL)**

```bash
# Автоматический бэкап каждый день в 2:00 AM
0 2 * * * pg_dump -U postgres nora_db > /backup/nora_db_$(date +\%Y\%m\%d).sql
```

**Способ 2: Использование расширения pg_basebackup**

```bash
pg_basebackup -D /backup/base_backup -Ft -z -P
```

**Способ 3: Docker + Volumes**

```dockerfile
# Автоматическое резервное копирование при запуске контейнера
```

#### Восстановление:

```bash
# Из дампа SQL
psql -U postgres nora_db < /backup/nora_db_backup.sql

# Из бинарного бэкапа
pg_restore -d nora_db /backup/base_backup.tar.gz
```

#### В документации:

- Описание процесса бэкапирования
- Расписание автоматических бэкапов
- Процедура восстановления
- Тестирование восстановления (демонстрация)

---

### 3. ✅ DevOps-практики (CI/CD)

#### Описание:

- Автоматизация деплоймента БД и приложения
- Автоматизированное тестирование
- CI/CD pipeline для полного цикла разработки

#### Реализация:

**Инструменты (на выбор):**

- GitHub Actions (встроен в GitHub)
- GitLab CI/CD (встроен в GitLab)
- Jenkins (самостоятельный сервер)
- Docker + Docker Compose (контейнеризация)

**Примерный pipeline:**

```
Git push
  ↓
1. Автоматическое тестирование (unit tests, integration tests)
  ↓
2. Проверка качества кода (linting, security scan)
  ↓
3. Сборка приложения (компиляция, bundling)
  ↓
4. Развертывание БД (миграции, создание таблиц)
  ↓
5. Развертывание приложения (deployment)
  ↓
6. Smoke tests (проверка основной функциональности)
  ↓
✅ Проект готов к использованию
```

#### Пример GitHub Actions (.github/workflows/ci-cd.yml):

```yaml
name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_DB: nora_db_test
          POSTGRES_PASSWORD: testpass
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
  
    steps:
    - uses: actions/checkout@v3
  
    - name: Set up Node.js / Python / Java (выбрать)
      uses: actions/setup-node@v3
      with:
        node-version: '18'
  
    - name: Install dependencies
      run: npm install
  
    - name: Run tests
      run: npm test
  
    - name: Run linter
      run: npm run lint
  
    - name: Deploy to staging
      run: npm run deploy:staging
      if: github.ref == 'refs/heads/develop'
  
    - name: Deploy to production
      run: npm run deploy:production
      if: github.ref == 'refs/heads/main'
```

#### Дополнительно:

- Docker для контейнеризации приложения
- Docker Compose для локального развертывания
- Инструкции по setup и deployment в README

---

## 📝 Документация (в процессе разработки)

Будет создана следующая документация:

1. **Пояснительная записка** (30-50 стр.)

   - Описание предметной области
   - Требования к системе
   - Описание архитектуры
   - Структура БД (с ER-диаграммой)
   - Описание REST API
   - Описание технологий (бэкап, CI/CD)
   - Инструкции по использованию
2. **Техническая документация**

   - ER-диаграмма (концептуальная модель)
   - Логическая модель (с типами данных)
   - DDL-скрипты для PostgreSQL
   - Примеры SQL-запросов (SELECT, INSERT, UPDATE, DELETE)
   - Описание views и индексов
3. **API документация**

   - Swagger / OpenAPI спецификация
   - Примеры request/response для каждого endpoint
   - Коды ошибок и их описание
   - Примеры использования (curl, Postman)
4. **README**

   - Быстрый старт (setup, run, deploy)
   - Архитектура и компоненты
   - Окружение и зависимости
   - Инструкции по развертыванию (Docker, CI/CD)
5. **Текст выступления**

   - Краткое описание проекта (3-5 минут)
   - Основные функции и преимущества
   - Демонстрация функциональности
   - Ответы на возможные вопросы

---

## 🚀 Итоги

**"Нора"** - это полнофункциональная система управления кинотеатром, которая продемонстрирует:

✅ Глубокое понимание проектирования БД (нормализация, целостность)
✅ Практическое применение PostgreSQL (DDL, DML, оптимизация)
✅ Разработка REST API для взаимодействия с БД
✅ Реализацию системы баллов и сложной бизнес-логики
✅ Автоматизацию бэкапирования и восстановления
✅ Применение DevOps-практик и CI/CD
✅ Полную документацию и отчетность
